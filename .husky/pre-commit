#!/bin/sh
set -e

if git diff --cached --name-only | grep -q '^apps/desktop/src/i18n/'; then
  echo "Checking i18n key consistency..."
  bun run check:i18n
fi

echo "Running staged linters and formatters..."
bun run lint:staged
